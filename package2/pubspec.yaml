name: package2
description: A sample command-line application with basic argument parsing.
version: 0.0.1

resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  args:
  phoenix_socket:

dev_dependencies:
  lints:
  test:
